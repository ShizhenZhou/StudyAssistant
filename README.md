# Study Assistant 📚

个人自用安卓应用：**拍照搜题 → AI 解答 → 错题整理**（面向理工科大学题目）。

## 功能

- 📷 **拍照解答**：相机拍照 → **框选出题目区域** → DeepSeek 视觉模型（deepseek-v4-flash-vision-exp）识别并解答
- 🖐 **框选**：拍照后可框选题目区域（四条边单独可拖），或整张图直接识别
- 🤖 **AI 解答**：分步推导，公式用 **LaTeX/KaTeX** 渲染（积分、分式、矩阵等正确排版）
- 🔄 **重新生成** / 💬 **接续提问**：答案区可重新生成，也可继续追问（多轮对话）
- 📚 **错题本**：本地保存题目+解答（Room），随时回看、删除
- 🔑 **API Key 应用内填写**：在 ⚙️ 设置里填自己的 DeepSeek Key（**Android Keystore 加密**存储，便于分享给朋友）

## 技术栈

Kotlin · Jetpack Compose · MVVM · Room · Retrofit/OkHttp · CameraX · WebView+KaTeX（公式渲染）· 阿里云 Maven 镜像

## 版本记录

### v0.3.6（当前）
- 🆕 **错题分类**：存题时弹窗选择「**暂不分类** / 已有分类 / ➕新建分类」；AI 解答时**自动推测科目**作为默认值（可改）
- 🆕 **AI 优先归入已有分类**：解题时把已有分类列表带给模型，若题目属于其中某个，直接用该分类名，避免重复新建同类分类
- 🆕 错题本顶部**分类筛选 chips**（全部 + 你建的分类），一键过滤
- 🆕 错题详情页**显示并修改分类**（顶部 📁 按钮）
- 🆕 分类用独立 `categories` 表存储（数据库升级 v5），为后续「知识点 tag + 针对性复习」打基础
- 🆕 错题本改为**两列瀑布流**（卡片高度随图片比例错开，新→旧从左到右、从上到下）
- 🆕 分类栏增加「**未分类**」标签，可单独查看暂不分类的错题
- ⏳ **批量改分类**：暂未实现，留待后续版本讨论
- 🆕 数据库迁移 v4→v5，保留已有错题（新增分类默认「暂不分类」）

### v0.3.5
- 🆕 **追问可带图**：追问输入框左侧改为**图库图标**，可从相册选 **1~3 张图**附在追问里，AI 同时看图与文字作答
- ✨ **输入栏样式**：圆角输入框 + 右侧蓝色**胶囊「发送」按钮**，更贴近常见聊天输入栏
- 🆕 追问消息气泡**文字下方显示附图缩略图**；附图随会话保存进错题本、回顾时也能看到
- 🔄 移除对话栏「📷 拍下一题」按钮；解题页**返回键直接回到拍题界面**（错题本回顾仍返回错题本）
- 🆕 拍题/批改相机支持**点击对焦 + 对焦框动画**；批改页快门改拍题同款、三键同水平；追问栏更紧凑

### v0.3.4
- 🆕 **批改题目**：主页新增入口；可切换**单张/两张拍摄**（题目+手写答案），AI 判断正误、指出错误步骤并针对性讲解
- 🆕 应用图标字母改为 **SA**
- 🔄 错题本删除改为**软删除**，确认后按钮变「↩ 恢复」可撤销
- ✨ 追问输入框**键盘避让**（输入时文本框在键盘上方）；拍题图库按钮与快门齐平
- 🐛 bug：状态栏预留空间 + 图标颜色跟随主题；网络断开显示「继续生成」重试

### v0.3.3
- 🆕 应用**图标**（白底 + 蓝书 + "SS"）；桌面名称 **Study Assistant**
- 🆕 存错题本**保存完整对话会话**，点击错题可加载并**继续问答**（续答）
- 🐛 bug：框选红色加粗、四角可任意方向拖拽同时改长宽；加载答案显示"答案生成中……"
- 🔧 APK 命名规范：`StudyAssistant-<版本>-<时间>.apk`

### v0.3.2
- 🆕 底部**双选项卡**：主页 / 设置
- 🎨 **应用主题**：浅色 / 深色 / 跟随系统（默认跟随系统）
- 🔑 **Key 设置**移入设置选项卡；关于页标注作者 zsz、DeepSeek-V4 辅助、版本号
- 🐛 bug：答案滚动修复（常驻滚动条、框内滚动）
- 🎨 解答改**聊天气泡**（回答灰底、追问浅绿底）；首次打开自动弹填 Key 对话框
- 🆕 拍题按钮重排（圆形快门居中 / 花瓣图库左下角）；错题本单击跳详情页看解答；存错题按钮带书本图标 + 可切换

### v0.3.1
- ✨ 答案区支持 **Markdown 排版**：`**加粗**`、`# 标题`、`- 列表`、行内代码等不再显示源码
- ✨ **长答案滚动修复**：WebView 自动测量高度、外层列表滑动，不再卡住
- ✨ **宽公式**：过宽自动缩小（最多 2 档）+ 公式内横向滚动，页面不再左右滑动
- 🆕 拍题界面加 **重新拍摄** 按钮
- 🆕 问答界面加 **拍下一题** 方形相机按钮
- 🆕 拍题界面 **🖼️ 相册搜题**（从相册选图，免存储权限）
- 🆕 解答页顶部 **可折叠原图**（显示框选出的图片，默认折叠，点击展开）
- 🆕 错题本**显示框选原图**，点开可查看 LaTeX 排版的解答（方便复习）
- 🔧 首页 **⚙️ 填Key** 按钮下移，更好点击
- 🔧 长解答改为**内嵌上下滚动**，内容超高时可完整翻阅

### v0.3.0
- 🆕 **拍题框选**：拍照后框选出题目区域（矩形框、四边单独可拖），或整张图识别
- 🆕 **重新生成 + 接续对话**：答案区可重新生成；底部输入框可继续追问（多轮对话流）
- 🆕 **API Key 应用内自填**：在 ⚙️ 设置里填 Key，**Android Keystore 加密**存储（不再写死，便于分享）
- 🆕 **KaTeX 公式渲染**：答案里的公式（积分、分式、矩阵）渲染为真正的数学排版

### v0.2（早期版本）
- 🔄 **改用 DeepSeek 处理图像**：把图片直接发给 DeepSeek 视觉模型识别并解答，**不再使用百度 OCR**

### v0.1（项目建立）
- 🆕 项目搭建：**百度智能云 OCR 识别图像 + DeepSeek 解题**，拍照搜题、AI 解答、错题整理雏形

## 构建

```bash
# 首次构建会自动下载依赖（走阿里云镜像加速）
gradlew.bat assembleDebug
# 产物: app/build/outputs/apk/debug/app-debug.apk
```

## 配置 API Key（应用内）

1. 打开 App → 右上角 **⚙️ 设置**
2. 粘贴你的 DeepSeek API Key（sk- 开头）→ 保存
3. Key 用 **Android Keystore 加密** 存于本机，不写入代码、不提交仓库

Key 获取：https://platform.deepseek.com（账号需开通视觉模型权限）

## 环境要求

- JDK 17+（本机 JDK 21）
- Android SDK（compileSdk 37，本机已装 android-37.0）
