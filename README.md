# Study Assistant 📚

个人自用安卓应用：**拍照搜题 → AI 解答 → 错题整理**（面向理工科大学题目）。

## 功能

- 📷 **拍照解答**：相机拍照 → **框选出题目区域** → DeepSeek 视觉模型（`deepseek-flash`，V4.1-Flash）识别并解答
- ✏️ **直接提问**：拍题界面右上角入口，可用**文字 + 1~3 张图**直接提问，不必先拍照
- 🖐 **框选**：拍照后可框选题目区域（四条边单独可拖），或整张图直接识别
- 🤖 **AI 解答**：分步推导，公式用 **LaTeX/KaTeX** 渲染（积分、分式、矩阵等正确排版）；答案底部附**核心知识点/难点**
- 🔄 **重新生成** / 💬 **接续提问**：可重新生成，也可多轮追问（**支持附带图片**）；可**编辑多选删除**消息
- ✏️ **批改题目**：单张/两张拍摄（题目+手写答案），AI 判断正误、指出错误步骤并针对性讲解
- 📚 **错题本**：本地保存（Room），**两列瀑布流**、**全文搜索**、**科目分类 + 知识点标签（多 tag）**、按分类/标签筛选、长按批量改分类/删除
- 📖 **复习（艾宾浩斯）**：新存错题自动进入按 `0/1/2/4/7/15/30` 天排期的复习队列；**今天/本周**选项卡、按科目→知识点分组；点「熟悉/模糊/忘记」自动跳下一题；可**练同类题**（AI 出题、可互动、点「查看答案」）
- 🔔 **复习提醒通知**：设置页可开每日提醒（自定义滚轮时间），到点通知「今天/本周还有 xx 道错题」；点击通知直达复习页
- 🔑 **API Key 应用内填写**：在 ⚙️ 设置里填自己的 DeepSeek Key（**Android Keystore 加密**存储）
- 🌐 **AI 生成语言可选**：设置 → 语言设置 → AI 生成语言（跟随系统 / 跟随题目 / 简繁中文 / English / 日本語 / 한국어 / Deutsch / Français / Español / Русский）
- 🌐 **界面多语言**：设置 → 语言设置 → 应用语言（跟随系统 / 简体中文 / 繁體中文 / English / 日本語 / 한국어），切换立即生效
- 💾 **数据管理**：导出/导入 JSON 备份（含图片）、清空数据；🔑 API 管理里可看累计调用次数与 token 用量

## 技术栈

Kotlin · Jetpack Compose · MVVM · Room · Retrofit/OkHttp · CameraX · WebView+KaTeX（公式渲染）· AlarmManager（复习提醒）· 阿里云 Maven 镜像

## 版本记录

### v0.5.3_beta（测试分支 streaming_output_test，当前）
- 🌊 **解题 / 追问流式输出**：答案边生成边显示（末尾 ▍ 光标），长推导不再整段白等；生成中可继续滚动阅读
- 🛟 **中断保留**：网络断开时保留已生成内容，并可点「继续生成」重试
- ⚠️ 本版是**测试版本**，只在分支 `streaming_output_test` 上构建；批改 / 同类题暂未启用流式；未发布 Release

### v0.5.2
- 🧮 **公式键盘改为窄条**：默认只显示一行小字「公式键盘」（22dp 高），点一下才展开符号行 + LaTeX 模板行，再点收起——不再常驻占用输入区空间
- 📱 **锁定竖屏**：`MainActivity` 加 `android:screenOrientation="portrait"`，转动手机不再跟随旋转（此前未锁定，横屏下部分页面排版会乱；大屏设备上系统可能仍忽略该锁定）

### v0.5.1
- 🔍 **错题本搜索改为按钮唤出**：标题栏右侧 🔍 按钮打开搜索框，点其他位置或返回键自动关闭（不再常驻占位）
- 🗂 **错题本「管理」**：科目（分类）纵向列表，可 **✏️ 重命名**、**勾选批量删除**；删除前二次确认，并可选**是否连同科目中的错题一起删除**（不勾选则题目改为「未分类」）

### v0.5.0
- 💾 **数据管理**（设置 → 💾 数据管理）：**导出备份**（单个 JSON，含题目图片与完整会话）/ **导入备份**（同名分类标签复用、题干相同自动跳过，不覆盖现有数据）/ **清空所有数据**（二次确认）
- ⚙️ **设置重构**：「关于」改为二级菜单（应用信息 + 更新内容）；「🔑 API 管理」= Key 设置 + **累计用量统计**（调用次数、输入/输出 tokens，可重置），输入框下标注「API Key 一律加密保存在本地」
- 📖 **复习默认折叠解答**：先自己回忆，再点「👁 看解答」展开；顶部显示「第 i/n 题」进度
- 🔍 **错题本全文搜索**：按题干（AI 转译文本）与解答检索
- 🧮 **公式快捷输入条**：直接提问与追问新增 ∫ ∑ √ π 等符号 + LaTeX 模板（分式/上下标/积分上下限）
- 🔌 **重做模式预留接口**：复习时手写重做 → 复用批改 → 自动更新掌握度（后续版本接入 UI）
- 🔍 **对话图片点击放大**：解题/批改/同类题气泡里的图片单击→全屏查看（双指缩放、拖动，✕ 或返回键关闭）
- 🛡 **清空数据需手动输入确认语**（确认键红色）；「API Key 加密保存」提示为绿色字

### v0.5.0_beta
- 🌐 **应用界面语言**：⚙️ 设置 → 🌐 语言设置 → **应用语言**（跟随系统 / 简体中文 / 繁體中文 / English / 日本語 / 한국어），**切换立即生效、无需重启**
- 🌐 **界面文案全面多语言**：主页、拍照、直接提问、解题、批改、错题本、复习、设置、更新内容、通知与错误提示全部本地化（160+ 条文案 × 4 张语言表；繁體中文由简体自动转换，含词级规则处理「关系/系统」「复习/恢复」等多义字）
- 🧩 **「AI 生成语言」与「应用语言」拆成两项独立设置**：前者决定 AI 用什么语言作答，后者决定 App 界面文字
- 🛠 **仓库安全**：`.gitignore` 补 adb 私钥规则；新增提交前密钥扫描（`tools/check-secrets.ps1` + `.githooks/pre-commit`）

### v0.4.9
- 🎨 **拍照题的题目气泡只显示原图**：不再显示 AI 转译的题干文字（题干照旧保存，错题本标题/搜索/编辑不受影响；旧会话同样只显示原图）；直接提问仍显示你自己写的问题
- 🌐 **新增「⚙️ 设置 → 🌐 语言设置 → AI 生成语言」**：跟随系统 / 跟随题目 / 简体中文 / 繁體中文 / English / 日本語 / 한국어 / Deutsch / Français / Español / Русский
- 🌐 **语言设置覆盖全部 AI 输出**：拍题解答、直接提问、追问、批改、同类题都按设定语言作答（数学公式仍是 LaTeX）

### v0.4.8
- 🎨 **题目/首次提问统一显示为浅绿色用户气泡**（含原图/附图）：单张拍题、两张拍题、直接提问、批改、错题本、复习界面全部一致；不再使用「顶部折叠原题 + AI 转译文字」的老方式
- 🎨 **复习界面改为单列**：整行宽度、题图更大更清楚
- 📦 **R8 混淆 + 资源压缩转为常规**（release 变体）：APK 约 4.4MB（原 30MB）；保留旧会话兼容（keep 规则）

### v0.4.7.5（临时版本）
- 📦 R8 代码裁剪/混淆 + 资源压缩（仅 release）；release 用 debug 签名，可直接覆盖安装
- 🆕 设置页新增二级菜单「📝 更新内容」：显示 v0.1 → 当前版本的更新记录

### v0.4.7
- 🆕 **直接提问会在对话里显示「我的提问 + 附图」**（与拍题后的问答一致：用户气泡带图，AI 回答在下）
- ⚡ **性能**：错题本/复习列表的图片改为**缓存 + 降采样解码**（避免每次重组解码全尺寸大图，滚动更流畅、内存更省）
- 📦 **体积**：删除 KaTeX 冗余 `.woff`（保留 woff2，约 -0.29MB）；预览工具改为仅 debug 依赖（正式包不打包）
- 🆕 **拍照搜题新增「单张 / 两张」拍摄模式**（与批改题目一致）：两张模式可拍两张作为同一道题一起解答
- ⚡ **更多效率优化（小改动）**：API Key 内存缓存（不再每次请求都做 Keystore 解密）；复习页 Flow 缓存；列表时间格式化器复用；对话消息列表缓存；WebView 内容未变时跳过重渲染；错题本筛选结果缓存；分类/标签名预缓存

### v0.4.6
- 🔄 「**关于**」不再做二级菜单，直接显示在设置主页（其余四项仍为二级菜单）
- 🆕 生成答案的通知**点击可回到正在生成的页面**（前台服务通知加 contentIntent）

### v0.4.5
- 🆕 **设置页改为二级菜单**：主页为入口列表（🔑 API Key / 🎨 应用主题 / 🔔 复习提醒 / 🔋 后台运行 / ℹ️ 关于），点进去再做具体设置；支持返回键回主页

### v0.4.4
- 🆕 **存题选标签改为下拉菜单**：点「选择标签」弹出**纵向可滑动**的标签列表（标签再多也能滚），仍保留 **AI 自动预选**
- 🆕 **错题本标签筛选改为选择框「按标签搜索」**：点击才弹出下拉列表，勾选（可多选）后筛选

### v0.4.3
- 🔄 **模型升级到 DeepSeek-V4.1-Flash**（`deepseek-flash`，原生多模态）：视觉与文本统一用该模型
- 原因：`deepseek-v4-flash-vision-exp` 已退役、`deepseek-v4-pro` 于 2026/09/14 起被路由到 V4.1-Flash
- 🐛 修复：解题页标题「解题」被右侧按钮挤压成省略号——**缩小操作按钮间距/字号**并给标题保留最小宽度（按钮文字保留）

### v0.4.2
- 🆕 **点击复习提醒通知**直接打开「复习」界面
- 🆕 设置页「**一键开启自启动**」（适配荣耀/华为/小米/OPPO/vivo，回退应用详情页）
- 🆕 拍题界面右上角「**✏️ 直接提问**」：可用文字 + 可选 1~3 张图直接提问，不必先拍照
- 🆕 通知时间为**系统上下滑动**选择（TimePicker）；设置页含忽略电池优化/自启动引导

### v0.4.1
- 🆕 **复习完自动跳下一题**：点「熟悉/模糊/忘记」后自动切到同科目下一题（没有则下一科目），今日全部复习完才退出并提示
- 🆕 **练同类题**：复习详情右上角改为「✏️ 练同类题」→ AI 出一道同类题（**先只显示题目**，可互动）；点「👁 查看答案」才显示答案
- 🆕 **复习提醒通知**：设置页新增「通知」——开关 + 提醒时间（每天一次）；到点通知「今天还有 xx 道错题要复习！本周末前还有 xx 道！」
- 🆕 通知权限（Android 13+ 申请）；开机自动恢复提醒

### v0.4.0
- 🆕 **复习功能**：主页新增「📖 复习」入口
- 🆕 **艾宾浩斯自动排期**：新存错题自动进入复习队列，按 0/1/2/4/7/15/30 天间隔调度（新增 `review` 表，DB v7）
- 🆕 复习界面**今天 / 本周**两个选项卡（本周=截止本周日到期）；按**科目→知识点**分组、两列瀑布流展示
- 🆕 点进错题 → 详情页底部变为 **🌱熟悉 / 🌤模糊 / 🔥忘记** 三按钮，点击改变下次出现时机（熟悉=+1档、模糊=保持、忘记=重置今天）
- 🆕 **近似题接口预留**：`StudyAssistant.generateSimilarQuestion()` 已留方法签名，后续接 UI
- 🆕 数据库 v6→v7；`DATA_SCHEMA.md` 同步

### v0.3.9
- 🆕 **知识点标签（tag）**：记录错题核心知识点，与科目分类解耦、**全局不分科**、多对多
- 🆕 **AI 自动打 tag**：解题时模型顺带输出「知识点：…」（≤5），并优先复用已有标签；存题/详情页**可改**（选已有 / 新建，≤5）
- 🆕 错题本顶部**标签多选筛选**（可同时选多个标签），卡片显示该题标签
- 🆕 数据库 **v5→v6**（新增 `tags` + `question_tags` 表）；`DATA_SCHEMA.md` 已同步
- ⏳ **按标签复习**功能后续实现

### v0.3.8
- 🐛 修复：对话正文回归**单个 WebView 稳定滚动**，与拍题界面滚动/显示控件一致，**错题本点进后上下滑动顺畅**（此前每条消息独立 WebView 导致高度测量、滑动跳动）
- 🐛 修复：错题**时间戳**改为记录提问时时间，点进去退出不再刷新
- 🗂 版本约定：今后**自定义数据文件**会在文件头写 **4 字节版本号**，供未来结构变化时自动迁移（当前错题/会话仍用 Room，已带迁移）
- 🔐 密钥策略：源码/仓库**不含任何 key**；真正 key 由用户在 App 内填写、存 Android Keystore
- ⏳ 追问**长按多选删除**：暂时移除（与稳定滚动冲突），后续用其它交互再实现

### v0.3.7
- 🆕 **批量改分类 / 批量删除**：错题本**长按**进入多选模式，单击勾选，顶部「📁 分类 / 🗑 删除 / ✓ 完成」
- 🆕 批量分类：弹分类对话框（已有 / 新建 / **暂不分类**）统一应用
- 🆕 批量删除：**彻底删除**（弹确认框，不可恢复）；单条删除仍为软删除
- 🆕 多选卡片显示勾选标记 + 选中绿框

### v0.3.6
- 🆕 **错题分类**：存题时弹窗选择「**暂不分类** / 已有分类 / ➕新建分类」；AI 解答时**自动推测科目**作为默认值（可改）
- 🆕 **AI 优先归入已有分类**：解题时把已有分类列表带给模型，若题目属于其中某个，直接用该分类名，避免重复新建同类分类
- 🆕 错题本顶部**分类筛选 chips**（全部 + 你建的分类），一键过滤
- 🆕 错题详情页**显示并修改分类**（顶部 📁 按钮）
- 🆕 错题本改为**两列瀑布流**（卡片高度随图片比例错开，新→旧从左到右、从上到下）
- 🆕 分类栏增加「**未分类**」标签，可单独查看暂不分类的错题
- 🆕 分类用独立 `categories` 表存储（数据库升级 v5），为后续「知识点 tag + 针对性复习」打基础
- 🆕 数据库迁移 v4→v5，保留已有错题（新增分类默认「暂不分类」）

### v0.3.5
- 🆕 **追问可带图**：追问输入框左侧改为**图库图标**，可从相册选 **1~3 张图**附在追问里，AI 同时看图与文字作答
- ✨ **输入栏样式**：圆角输入框 + 右侧蓝色**胶囊「发送」按钮**，更贴近常见聊天输入栏
- 🆕 追问消息气泡**文字下方显示附图缩略图**；附图随会话保存进错题本、回顾时也能看到
- 🔄 移除对话栏「📷 拍下一题」按钮；解题页**返回键直接回到拍题界面**（错题本回顾仍返回错题本）
- 🆕 拍题/批改相机支持**点击对焦 + 对焦框动画**；批改页快门改拍题同款、三键同水平；追问栏更紧凑

### v0.3.4
- 🆕 **批改题目**：主页新增入口；可切换**单张/两张拍摄**（题目+手写答案），AI 判断正误、指出错误步骤并针对性讲解
- 🆕 应用图标字母改为 **SA**
- 🔄 错题本删除改为**软删除**，确认后按钮变「↩ 恢复」可撤销
- ✨ 追问输入框**键盘避让**（输入时文本框在键盘上方）；拍题图库按钮与快门齐平
- 🐛 bug：状态栏预留空间 + 图标颜色跟随主题；网络断开显示「继续生成」重试

### v0.3.3
- 🆕 应用**图标**（白底 + 蓝书 + "SS"）；桌面名称 **Study Assistant**
- 🆕 存错题本**保存完整对话会话**，点击错题可加载并**继续问答**（续答）
- 🐛 bug：框选红色加粗、四角可任意方向拖拽同时改长宽；加载答案显示"答案生成中……"
- 🔧 APK 命名规范：`StudyAssistant-<版本>-<时间>.apk`

### v0.3.2
- 🆕 底部**双选项卡**：主页 / 设置
- 🎨 **应用主题**：浅色 / 深色 / 跟随系统（默认跟随系统）
- 🔑 **Key 设置**移入设置选项卡；关于页标注作者 zsz、DeepSeek-V4 辅助、版本号
- 🐛 bug：答案滚动修复（常驻滚动条、框内滚动）
- 🎨 解答改**聊天气泡**（回答灰底、追问浅绿底）；首次打开自动弹填 Key 对话框
- 🆕 拍题按钮重排（圆形快门居中 / 花瓣图库左下角）；错题本单击跳详情页看解答；存错题按钮带书本图标 + 可切换

### v0.3.1
- ✨ 答案区支持 **Markdown 排版**：`**加粗**`、`# 标题`、`- 列表`、行内代码等不再显示源码
- ✨ **长答案滚动修复**：WebView 自动测量高度、外层列表滑动，不再卡住
- ✨ **宽公式**：过宽自动缩小（最多 2 档）+ 公式内横向滚动，页面不再左右滑动
- 🆕 拍题界面加 **重新拍摄** 按钮
- 🆕 问答界面加 **拍下一题** 方形相机按钮
- 🆕 拍题界面 **🖼️ 相册搜题**（从相册选图，免存储权限）
- 🆕 解答页顶部 **可折叠原图**（显示框选出的图片，默认折叠，点击展开）
- 🆕 错题本**显示框选原图**，点开可查看 LaTeX 排版的解答（方便复习）
- 🔧 首页 **⚙️ 填Key** 按钮下移，更好点击
- 🔧 长解答改为**内嵌上下滚动**，内容超高时可完整翻阅

### v0.3.0
- 🆕 **拍题框选**：拍照后框选出题目区域（矩形框、四边单独可拖），或整张图识别
- 🆕 **重新生成 + 接续对话**：答案区可重新生成；底部输入框可继续追问（多轮对话流）
- 🆕 **API Key 应用内自填**：在 ⚙️ 设置里填 Key，**Android Keystore 加密**存储（不再写死，便于分享）
- 🆕 **KaTeX 公式渲染**：答案里的公式（积分、分式、矩阵）渲染为真正的数学排版

### v0.2（早期版本）
- 🔄 **改用 DeepSeek 处理图像**：把图片直接发给 DeepSeek 视觉模型识别并解答，**不再使用百度 OCR**

### v0.1（项目建立）
- 🆕 项目搭建：**百度智能云 OCR 识别图像 + DeepSeek 解题**，拍照搜题、AI 解答、错题整理雏形

## 密钥与安全约定

- **仓库里绝不出现任何密钥**。`.gitignore` 已覆盖 `*.key`、`*.jks`、`*.keystore`、`*.pem`、`secrets.properties`、`local.properties`、`.env`、`LOCAL_SECRETS.txt`，以及 adb 调试私钥（`.adb-keys/`、`adbkey`、`adbkey.pub`）。
- **adb 调试私钥的位置**：本项目所在的**工作区根目录**（即仓库之外的 `..\.adb-keys\`），用于免授权连接调试手机。**不要把它拷进仓库**——`.gitignore` 里那三条是防御性忽略，防的就是这种手滑。
- **应用内的 API Key**：运行时由使用者在本 App 设置页填写，用 Android Keystore 加密存于手机；代码与仓库中不含任何真实 Key（`git grep` 全量扫描过 `sk-`/`AKID` 等模式，为空）。
- **提交前本地扫描（建议一次性开启）**：

  ```bash
  git config core.hooksPath .githooks
  ```

  之后每次 `git commit` 都会先扫描暂存区：文件名命中密钥名单、或内容里出现 `sk-…`、私钥头、各类云厂商 AK 模式时**直接拦下**（确认不是密钥可用 `git commit --no-verify` 绕过）。

  手动运行（不提交也能查）：`powershell -NoProfile -ExecutionPolicy Bypass -File tools/check-secrets.ps1`

- **GitHub 端说明**：本仓库是**私有仓库**，GitHub 的 secret scanning / push protection 属于付费的 *GitHub Secret Protection*，免费版开不了（实测 REST API 返回 `422 Secret scanning is not available for this repository`），所以用上面的本地钩子兜底。

### 一键提交并推送：`tools/git-sync.ps1`

```powershell
# 扫描密钥 → 提交 → 推送 → 核对（一步到位）
.\tools\git-sync.ps1 -Message "feat(v0.5.3): 说明本次改动"

.\tools\git-sync.ps1 -Message "docs: 只提交不推送" -NoPush
.\tools\git-sync.ps1 -NoCommit                      # 工作区已提交，只推送
```

它解决的问题（在受限/无 `sh` 的环境里尤其有用）：

1. **提交前先跑 `tools/check-secrets.ps1`** —— 于是沙箱等环境里 `sh` 型 git 钩子跑不起来也不影响安全兜底；
2. **凭据走 `GIT_ASKPASS`**（原生 `.cmd`，不经 `sh`），token 只在环境变量中，**不进入命令行**（因此无需 `https://<token>@…` 那种写法，也不会写进 shell 历史）；
3. **传输自动选路**：直连优先，失败自动切本地代理（`-ProxyPort`，默认 12450），并**记住上次成功的方式**（`.git/dsh-transport`），下次直接用它；
4. 推送后自动同步 `refs/remotes/<remote>/<branch>`，并打印「本地 HEAD / 远端 HEAD / 是否一致」。

退出码：`0` 成功；`1` 失败（扫描未过 / 提交失败 / 推送失败）。

## 构建

```bash
# 首次构建会自动下载依赖（走阿里云镜像加速）
gradlew.bat assembleDebug
# 产物: app/build/outputs/apk/debug/app-debug.apk
```

## 配置 API Key（应用内）

1. 打开 App → 右上角 **⚙️ 设置**
2. 粘贴你的 DeepSeek API Key（sk- 开头）→ 保存
3. Key 用 **Android Keystore 加密** 存于本机，不写入代码、不提交仓库

Key 获取：https://platform.deepseek.com（账号需开通视觉模型权限）

## 环境要求

- JDK 17+（本机 JDK 21）
- Android SDK（compileSdk 37，本机已装 android-37.0）
