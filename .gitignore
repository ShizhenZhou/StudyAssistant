# Gradle
.gradle/
build/
.kotlin/

# Android / IDE
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/

# 密钥/令牌 - 绝不提交（无论命名）
secrets.properties
*.keystore
*.jks
# 发布签名（2026-09-25 起改用「自有证书 + key rotation lineage」签发）
# 真正的 keystore 与 lineage.bin 都放在**仓库外**（工作区 keys\），这里再兜一层防手滑拷进来
*.p12
*.pfx
lineage*.bin
keys/
.keys/
local_secrets*
LOCAL_SECRETS.txt
local_secrets.txt
local_secret
*.secret
*.key
*.pem
api_key*
.env

# adb 调试私钥（本地保留在仓库之外；万一被拷进仓库也绝不提交）
.adb-keys/
adbkey
adbkey.pub

# APK 输出
*.apk
*.aab

# 构建日志（统一放项目内，也不提交）
*.log
build-logs/

