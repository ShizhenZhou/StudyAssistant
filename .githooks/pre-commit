#!/bin/sh
# git 提交前钩子：扫描暂存区里的密钥（逻辑复用 tools/check-secrets.ps1，便于跨平台维护）
# 启用方式（一次性）：git config core.hooksPath .githooks
# 临时绕过：git commit --no-verify
root=$(git rev-parse --show-toplevel) || exit 0
exec powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$root/tools/check-secrets.ps1"
